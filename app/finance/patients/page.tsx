// Finance portal patient lists (OPD | IPD). Renders the exact same tabbed dashboard
// used by reception so finance staff can look up patients, with a single source of
// truth — any change to the reception dashboard applies here automatically. The
// component detects the /finance/patients route and titles itself "Patients".
export { default } from '@/app/reception/dashboard/page';
