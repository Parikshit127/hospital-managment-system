{
    "framework": "nextjs",
    "buildCommand": "npx prisma generate && next build",
    "installCommand": "npm ci --no-audit --no-fund",
    "regions": ["bom1"],
    "crons": [
        {
            "path": "/api/ipd/bed-cleaning-sla",
            "schedule": "0 0 * * *"
        }
    ]
}
