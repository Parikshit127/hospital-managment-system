# Skip Puppeteer's Chrome download — we use puppeteer-core + @sparticuz/chromium instead.
# Without this, every `npm install` tries to download ~280 MB of Chrome
# and fails on low-disk machines.
puppeteer_skip_download=true
puppeteer_skip_chromium_download=true
